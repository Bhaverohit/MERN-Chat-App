// import io from "socket.io-client";

// const url = "http://localhost:8000";

// const socket = io(url);

// // Cleanup function to disconnect socket when the application exits
// window.addEventListener("beforeunload", () => {
//     socket.disconnect();
// });

// export default socket;
